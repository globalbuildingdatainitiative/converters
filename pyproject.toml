[project]
name = "converters"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "lcax==2.5.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
